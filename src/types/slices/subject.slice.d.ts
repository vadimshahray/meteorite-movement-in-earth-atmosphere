/** Состояние слайса исследуемого объекта */
type SubjectSliceState = {}

/**
 * Слайс, отвечающий за исследуемый объект
 * @interface
 */
interface SubjectSlice extends SliceCaseReducers<SubjectSliceState> {}
